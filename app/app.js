'use strict';

// const fs = require('fs');
// const path = require('path');

module.exports = app => {
  // 使用 app 对象
  console.log(app);
  // const targetDir = path.join(this.config.baseDir, '/public/imgUpload');
};
